# @cleanqueue/api

Phase 1 placeholder package.

The OAuth waitlist and static frontend continue to run via the root `server.js` for Netlify/Render compatibility. This package will host the modular REST API in a later phase.
