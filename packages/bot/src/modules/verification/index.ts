import { createPlaceholderModule } from '../placeholder';

export const verificationModule = createPlaceholderModule(
  'verification',
  'Verification',
  'Age verification, captcha, onboarding gates — Phase 2',
);
