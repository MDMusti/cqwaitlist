import { createPlaceholderModule } from '../placeholder';

export const voiceModule = createPlaceholderModule(
  'voice',
  'Voice',
  'Voice channel management, temp channels, activity — Phase 3',
);
