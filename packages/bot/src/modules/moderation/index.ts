import { createPlaceholderModule } from '../placeholder';

export const moderationModule = createPlaceholderModule(
  'moderation',
  'Moderation',
  'Warns, mutes, kicks, bans, case management — Phase 2',
);
